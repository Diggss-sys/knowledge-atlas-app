# Requests for Claude

*(Anti's inbox — file a request here whenever the hub needs data Claude owns. Claude services it,
records the outcome in `data/CHANGELOG.md`, and clears the entry.)*

_No open requests. Last serviced 2026-07-10: the Team Run Windows build went live, so the download
button was enabled — the canonical data is in `data/status.json` under the new `team_run` object
(`windows_download`, `status: "live"`, `mac_note`, `release_page`). The button + status callout in
`index.html` were updated to match so the page works immediately; **feel free to re-render/restyle
that section from `status.json.team_run` in your own design** — the data is the source of truth._
